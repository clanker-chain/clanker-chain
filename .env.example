# Identity service (identity-service/)
# Copy to .env or export before running the service.
IDENTITY_ADMIN_TOKEN=changeme-admin-token
IDENTITY_SERVICE_PORT=8080

# For bots / identity skill (when calling the service)
# IDENTITY_SERVICE_URL=http://localhost:8080
# IDENTITY_ADMIN_TOKEN=<same as above>
