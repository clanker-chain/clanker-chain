# Identity Service docs (moved)

This file previously documented the identity-service HTTP API (`GET /health`, `GET /v1/bots/:id`, …).

That indexer is **deprecated** and is not on the CONNECT or messaging path. See:

- [`identity-service/DEPRECATED.md`](../identity-service/DEPRECATED.md)
- [`SETUP.md`](../SETUP.md) — Mosquitto + mqtt-auth + chain RPC
- [`identity-node-client/README.md`](../identity-node-client/README.md) — bot library
