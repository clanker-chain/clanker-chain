# Identity HTTP API (deprecated)

The read-only indexer HTTP API (`GET /health`, `GET /v1/operators/:id`, `GET /v1/bots/:id`) is **not** part of hub runtime.

Use on-chain reads via `@clanker-chain/identity-node-client` and see [`identity-service/DEPRECATED.md`](../../identity-service/DEPRECATED.md).
