# After Sepolia deploy: copy to identity-service/.env (or merge into it).
#   cp .env.sepolia.example .env

CHAIN_RPC_URL=https://sepolia.base.org
REGISTRY_ADDRESS=0xYourDeployedRegistryHere
DEPLOYMENT_BLOCK=0
IDENTITY_SERVICE_PORT=8080
EVM_POLL_MS=2000
