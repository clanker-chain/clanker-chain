# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example
config/mosquitto-passwords

# Mosquitto runtime (referenced by config)
data/
logs/

# OS and editors
.DS_Store
*.log
*.swp
*~

# Optional: uncomment to stop tracking the materialized dev snapshot (Anvil test addresses).
# identity/bot-identity-ledger.json

# Foundry build artifacts (chain/)
chain/cache/
chain/out/
chain/broadcast/
chain/.anvil-state.json

# Stray self-referential symlinks (accidentally created by local ci-local runs)
/identity-node-client/identity-node-client
/mqtt-node-client/mqtt-node-client

identity/bot-identity-ledger.json